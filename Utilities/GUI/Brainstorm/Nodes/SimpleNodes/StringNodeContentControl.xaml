<UserControl x:Class="Utilities.GUI.Brainstorm.Nodes.SimpleNodes.StringNodeContentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    >
  <Border CornerRadius="5" BorderBrush="DarkBlue" BorderThickness="1">
    <Viewbox Name="ObjViewbox" Stretch="Uniform">
      <Grid Name="GridText">
        <TextBlock Name="TxtOverview" FontSize="32" Padding="5" TextWrapping="Wrap" Text="{Binding Text}" ToolTip="{Binding Text}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox Name="TxtEdit" FontSize="28" Padding="5" TextWrapping="Wrap" Text="{Binding Text}" HorizontalAlignment="Center" VerticalAlignment="Center" AcceptsReturn="True" AcceptsTab="True" />
      </Grid>
    </Viewbox>
  </Border>
</UserControl>
